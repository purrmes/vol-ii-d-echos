# Environment for NPP-Optimized WordPress Setup                                    #
# ----------------------------------------------------------------------------     #
# Author: [Hasan ÇALIŞIR]                                                          #
# Purpose: Full-stack Dockerized environment for NPP WordPress plugin, including   #
#          WordPress, PHP-FPM, Nginx, MySQL, WP-CLI, and necessary PHP extensions. #
# ----------------------------------------------------------------------------     #
# PRODUCTION CONFIGURATION - UPDATE ALL PASSWORDS AND SECRETS BEFORE DEPLOYMENT    #
# ----------------------------------------------------------------------------     #
# INSTRUCTIONS:                                                                     #
# 1. Copy this file to .env: cp .env.example .env                                  #
# 2. Update all placeholder values with your actual configuration                  #
# 3. Never commit .env file to version control                                     #
# ----------------------------------------------------------------------------     #

# External MariaDB Database Settings (Deployed Separately)
# Update with your actual MariaDB server hostname and credentials
export WORDPRESS_DB_HOST=your-mariadb-hostname:3306
export WORDPRESS_DB_NAME=your_database_name
export WORDPRESS_DB_USER=your_db_user
export WORDPRESS_DB_PASSWORD=${DB_PASSWORD}
export WORDPRESS_TABLE_PREFIX=wp_

# External Valkey Cache Settings (Deployed Separately - Redis Compatible)
# Update with your actual Valkey/Redis server hostname and credentials
export VALKEY_HOST=your-valkey-hostname
export VALKEY_PORT=6379
export VALKEY_PASSWORD=${VALKEY_PASSWORD}
export VALKEY_DATABASE=0

# Wordpress Settings
export WORDPRESS_HOME=wordpress

# Nginx Settings
export NGINX_CACHE=nginx_cache
export NGINX_CONF=./nginx/nginx.conf
export NPP_NGINX_CONF=./nginx/default.conf
export NPP_NGINX_PARAMS_CONF=./nginx/fastcgi_params
# IMPORTANT: Update with your actual domain name
export NPP_HTTP_HOST=example.com

# Traefik Proxy Settings
# Update with your desired Traefik router name (use alphanumeric and hyphens)
export TRAEFIK_ROUTER_NAME=your-site-name

# WP-CLI Settings
# These are used for initial WordPress setup (optional - can be done via UI)
export WORDPRESS_SITE_URL_="https://${NPP_HTTP_HOST}"
export WORDPRESS_SITE_TITLE_="Your Site Title"
export WORDPRESS_ADMIN_USER_=admin
export WORDPRESS_ADMIN_PASSWORD_=change-this-password
export WORDPRESS_ADMIN_EMAIL_=admin@example.com

# FPM Settings
export NPP_FPM_CONF=./fpm/www.conf
export FPM_DOCKER_CONF=./fpm/zz-docker.conf

# PHP Settings
export NPP_PHP_CONF=./php/npp.ini

# NPP Specific Settings
# These default values typically don't need to be changed
export NGINX_WEB_USER_=nginx
export NPP_NGINX_CACHE_PATH_=/var/cache/nginx
export NPP_WEB_ROOT_=/var/www/html
export NPP_USER_=npp
export NPP_UID_=18978
export NPP_GID_=33749
